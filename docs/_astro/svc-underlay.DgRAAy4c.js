import{o as t,t as e,u as n}from"./lit.BV3SxJOV.js";function s(t,e){if(!t||"string"!=typeof t||!t.includes("var("))return t;const n=t.match(/var\(\s*(--[\w-]+)\s*(?:,\s*(.+))?\)/);if(!n)return t;const[,s,i]=n;return(e?getComputedStyle(e).getPropertyValue(s).trim():"")||i?.trim()||t}function i(t,e,n){if(t=(t%360+360)%360/360,e=Math.min(1,Math.max(0,e/100)),n=Math.min(1,Math.max(0,n/100)),0===e)return[n,n,n];const s=n<.5?n*(1+e):n+e-n*e,i=2*n-s,a=t=>(t<0&&(t+=1),t>1&&(t-=1),t<1/6?i+6*(s-i)*t:t<.5?s:t<2/3?i+(s-i)*(2/3-t)*6:i);return[a(t+1/3),a(t),a(t-1/3)]}function a(t,e,n){const s=Math.max(1,Math.min(7,e||1)),i=function(t){if(!t||"string"!=typeof t)return{h:230,s:60,l:65};let e=0,n=0,s=0;const i=t.trim();if(i.startsWith("#"))4===i.length?(e=parseInt(i[1]+i[1],16),n=parseInt(i[2]+i[2],16),s=parseInt(i[3]+i[3],16)):(e=parseInt(i.substring(1,3),16),n=parseInt(i.substring(3,5),16),s=parseInt(i.substring(5,7),16));else{if(!i.startsWith("rgb"))return{h:230,s:60,l:65};{const t=i.match(/\d+/g);t&&([e,n,s]=t.map(Number))}}e/=255,n/=255,s/=255;const a=Math.max(e,n,s),o=Math.min(e,n,s);let r,l,h=(a+o)/2;if(a===o)r=l=0;else{const t=a-o;switch(l=h>.5?t/(2-a-o):t/(a+o),a){case e:r=(n-s)/t+(n<s?6:0);break;case n:r=(s-e)/t+2;break;default:r=(e-n)/t+4}r/=6}return{h:360*r,s:100*l,l:100*h}}(t),a=[{h:i.h,s:Math.min(76,Math.max(i.s,18)),l:Math.min(i.l,54)}];for(let o=1;o<s;o++){let t,e,r;if(n){const n=s-1,a=Math.min(100,16*n),l=(1===n?.6*a:2*a*(o-1)/(n-1)-a)+(12*Math.random()-6);t=(i.h+l+360)%360,e=Math.min(76,Math.max(38,i.s+(20*Math.random()-10))),r=Math.min(90,Math.max(52,i.l+(20*Math.random()-10)))}else{const n=s-1,a=Math.min(38,7*n),l=(1===n?.6*a:2*a*(o-1)/(n-1)-a)+(10*Math.random()-5),h=1===n?-.6:2*(o-1)/(n-1)-1,d=h*(h>=0?32:35)*Math.min(1,6*n/Math.max(32,35))+(4*Math.random()-2);t=(i.h+d+360)%360,e=Math.min(76,i.s+(16*Math.random()-8)),r=Math.min(92,Math.max(32,i.l+l))}a.push({h:t,s:e,l:r})}return a}var o=new Set,r=0,l=0,h=60;function d(t){r=0;const e=l?t-l:16.7;if(l=t,e>0){h+=.1*(1e3/e-h)}for(const n of Array.from(o))n._tick(t)||o.delete(n);o.size?r=requestAnimationFrame(d):l=0}var c={add(t){o.add(t),r||(r=requestAnimationFrame(d))},remove(t){o.delete(t)},getFps:()=>h},u=["low","medium","high"];var p={dots:0,bubbles:1,stars:2,leaf:3,snowflake:4},_={low:{particleBase:50,dprCap:1,glow:!1},medium:{particleBase:130,dprCap:1.5,glow:!0},high:{particleBase:260,dprCap:2,glow:!0}},f={dots:2,bubbles:1,stars:1,leaf:1,snowflake:1},m=null;function b(t,e,n){const s=t.createShader(e);if(t.shaderSource(s,n),t.compileShader(s),!t.getShaderParameter(s,t.COMPILE_STATUS)){const e=t.getShaderInfoLog(s);throw t.deleteShader(s),new Error(`Shader compile error: ${e}`)}return s}function g(t,e,n){const s=b(t,t.VERTEX_SHADER,e);let i;try{i=b(t,t.FRAGMENT_SHADER,n)}catch(o){throw t.deleteShader(s),o}const a=t.createProgram();if(t.attachShader(a,s),t.attachShader(a,i),t.linkProgram(a),!t.getProgramParameter(a,t.LINK_STATUS)){const e=t.getProgramInfoLog(a);throw t.deleteProgram(a),t.deleteShader(s),t.deleteShader(i),new Error(`Program link error: ${e}`)}return t.deleteShader(s),t.deleteShader(i),a}function v(t){t.enable(t.BLEND),t.blendFuncSeparate(t.ONE_MINUS_DST_COLOR,t.ONE,t.ONE,t.ONE_MINUS_SRC_ALPHA)}function y(t){const e=t.getExtension("WEBGL_lose_context");e&&e.loseContext()}var w=new Float32Array([-1,-1,3,-1,-1,3]);var x=null,M=0;function S(){1===++M&&((x=document.createElement("div")).style.cssText=["position:fixed","top:8px","left:50%","transform:translateX(-50%)","z-index:2147483647","padding:2px 8px","border-radius:999px","font:600 11px/1.6 monospace","color:#0f0","background:rgba(0,0,0,0.65)","pointer-events:none","letter-spacing:.02em"].join(";"),x.textContent="FPS: --",document.body.appendChild(x));let t=!0;return()=>{t&&(t=!1,0===(M=Math.max(0,M-1))&&(x?.remove(),x=null))}}var C=class extends e{static properties={theme:{type:String},tint:{type:String},deg:{type:Number},concept:{type:String},density:{type:Number},limit:{type:Number},speed:{type:Number},size:{type:String},quality:{type:String},fixed:{type:Boolean,reflect:!0},rounded:{type:String},debug:{type:Boolean},push:{type:Boolean},pushRadius:{type:Number},pushStrength:{type:Number},trail:{type:Number},blur:{type:Boolean,reflect:!0},gradient:{type:Boolean},distance:{type:Number},blobType:{type:String},total:{type:Number},colorful:{type:Boolean},blobMove:{type:String}};static styles=[n(":host{pointer-events:none;--bg-blob-opacity:.5;--bg-blob-blend:screen;--bg-blob-blur:120px;--bg-spatial-border:#ffffff1a;--bg-spatial-bg:#ffffff0d;display:block;position:absolute;inset:0;overflow:hidden}:host([data-theme=light]){--bg-blob-opacity:.35;--bg-blob-blend:multiply}:host([fixed]){position:fixed}:host([fixed]) .blob{width:85vmax;height:85vmax}:host([blur]) .underlay-wrapper{box-sizing:border-box;backdrop-filter:blur(.75rem);background:var(--bg-spatial-bg)!important}.underlay-wrapper{width:100%;height:100%;position:absolute;inset:0;overflow:hidden}.blob{width:100%;height:100%;filter:blur(var(--bg-blob-blur));opacity:var(--bg-blob-opacity);mix-blend-mode:var(--bg-blob-blend);pointer-events:none;border-radius:50%;position:absolute;transform:translate(-50%,-50%)}.blob-ellipse{pointer-events:none;position:absolute;inset:-20%}.underlay-canvas{opacity:0;width:100%;height:100%;transition:opacity .35s;display:block;position:absolute;inset:0}.underlay-canvas.is-visible{opacity:1}")];constructor(){super(),this.theme="",this.tint="#6d8dff",this.deg=0,this.concept="",this.density=null,this.limit=0,this.speed=1,this.size="1~3",this.quality="",this.fixed=!1,this.rounded="0px",this.debug=!1,this.push=!1,this.pushRadius=140,this.pushStrength=46,this.trail=0,this.blur=!1,this.gradient=!1,this.blobType="circleOverlap",this.distance=86,this.total=3,this.colorful=!1,this.blobMove="",this._blobTints=[],this._canvas=null,this._glCtx=null,this._program=null,this._buffer=null,this._particleCount=0,this._locs=null,this._fadeProgram=null,this._fadeBuffer=null,this._fadeLoc=null,this._dpr=1,this._qualityCtl=null,this._lastQualitySample=0,this._io=null,this._ro=null,this._themeObserver=null,this._visible=!1,this._destroyTimer=null,this._unregisterDebug=null,this._prefersReducedMotion=!1,this._pointerX=-9999,this._pointerY=-9999,this._pointerXEased=-9999,this._pointerYEased=-9999,this._rect=null,this._scrollPending=!1,this._handlePointerMove=this._handlePointerMove.bind(this),this._handleScroll=this._handleScroll.bind(this),this._handleContextLost=this._handleContextLost.bind(this),this._handleContextRestored=this._handleContextRestored.bind(this)}connectedCallback(){super.connectedCallback(),this._prefersReducedMotion=window.matchMedia?.("(prefers-reduced-motion: reduce)").matches??!1;const t=navigator.hardwareConcurrency||4,e=t<=2?"low":t<=6?"medium":"high";this._qualityCtl=function(t){let e=Math.max(0,u.indexOf(t)),n="auto",s=0,i=0;return{get tier(){return u[e]},setManual(t){const a=u.indexOf(t);-1!==a&&(n="manual",e=a,s=0,i=0)},setAuto(){n="auto",s=0,i=0},sample:t=>"auto"===n&&(t<45?(s++,i=0):t>55?(i++,s=0):(s=0,i=0),s>=2&&e>0?(e--,s=0,!0):i>=3&&e<u.length-1&&(e++,i=0,!0))}}(this.quality||e),this.quality&&this._qualityCtl.setManual(this.quality),this._syncTheme(),this._themeObserver=new MutationObserver(()=>this._syncTheme()),this._themeObserver.observe(document.documentElement,{attributes:!0,attributeFilter:["data-theme"]}),this._io=new IntersectionObserver(t=>this._onIntersect(t[0]?.isIntersecting??!1),{threshold:0}),this._io.observe(this),this.debug&&(this._unregisterDebug=S())}disconnectedCallback(){super.disconnectedCallback(),this._themeObserver?.disconnect(),this._io?.disconnect(),this._ro?.disconnect(),window.removeEventListener("pointermove",this._handlePointerMove),window.removeEventListener("scroll",this._handleScroll,{capture:!0}),clearTimeout(this._destroyTimer),c.remove(this),this._teardownGL(),this._unregisterDebug?.(),this._unregisterDebug=null}willUpdate(t){var e,n,i;this.gradient&&(t.has("gradient")||t.has("tint")||t.has("total")||t.has("colorful")||t.has("theme"))&&(this._blobTints=(e=s(this.tint,this),n=this.total,i=this.colorful,a(e,n,i)))}updated(t){t.has("theme")&&(this.theme?this.setAttribute("data-theme",this.theme):this.removeAttribute("data-theme"));const e=this.shadowRoot?.querySelector(".underlay-canvas")||null;e&&e!==this._canvas&&(this._canvas=e,this._canvas.addEventListener("webglcontextlost",this._handleContextLost),this._canvas.addEventListener("webglcontextrestored",this._handleContextRestored),this._visible&&this._initGL()),t.has("debug")&&(this._unregisterDebug?.(),this._unregisterDebug=this.debug?S():null),t.has("quality")&&(this.quality?this._qualityCtl?.setManual(this.quality):this._qualityCtl?.setAuto(),this._glCtx&&(this._onResize(),this._rebuildParticles(),this._visible&&c.add(this))),(t.has("tint")||t.has("total")||t.has("colorful")||t.has("density")||t.has("limit")||t.has("concept")||t.has("theme"))&&(this.concept?this._glCtx?(this._rebuildParticles(),this._visible&&c.add(this)):this._canvas&&this._visible&&this._initGL():this._glCtx&&(c.remove(this),this._teardownGL())),this._canvas&&t.has("push")&&this._syncPointerListener()}_syncTheme(){const t=document.documentElement.getAttribute("data-theme")||"";this.hasAttribute("theme")||(this.theme=t)}_onIntersect(t){this._visible=t,t?(clearTimeout(this._destroyTimer),this._destroyTimer=null,this._canvas&&!this._glCtx&&this._initGL(),this._canvas&&this._canvas.classList.add("is-visible"),this._glCtx&&c.add(this),this._syncPointerListener()):(this._canvas&&this._canvas.classList.remove("is-visible"),this._syncPointerListener(),clearTimeout(this._destroyTimer),this._destroyTimer=setTimeout(()=>{this._visible||(c.remove(this),this._teardownGL())},850))}_syncPointerListener(){const t=this._visible&&this.push;window.removeEventListener("pointermove",this._handlePointerMove),window.removeEventListener("scroll",this._handleScroll,{capture:!0}),t&&(window.addEventListener("pointermove",this._handlePointerMove),window.addEventListener("scroll",this._handleScroll,{passive:!0,capture:!0}),this._rect=this._canvas?.getBoundingClientRect()??null)}_handlePointerMove(t){if(!this._rect)return;const e=this._dpr||1;this._pointerX=(t.clientX-this._rect.left)*e,this._pointerY=(t.clientY-this._rect.top)*e}_handleScroll(){!this._scrollPending&&this._canvas&&(this._scrollPending=!0,requestAnimationFrame(()=>{this._scrollPending=!1,this._canvas&&(this._rect=this._canvas.getBoundingClientRect())}))}_handleContextLost(t){t.preventDefault(),c.remove(this),this._glCtx=null,this._program=null,this._buffer=null}_handleContextRestored(){this._visible&&this._initGL()}_initGL(){if(this._glCtx)return;if(!this.concept)return;if(!function(){if(null!==m)return m;try{const t=document.createElement("canvas");m=!(!t.getContext("webgl2")&&!t.getContext("webgl"))}catch{m=!1}return m}()||!this._canvas)return;const t=function(t){const e={alpha:!0,antialias:!1,premultipliedAlpha:!0,powerPreference:"low-power",preserveDrawingBuffer:!0};let n=t.getContext("webgl2",e),s=!0;return n||(n=t.getContext("webgl",e),s=!1),n?{gl:n,isWebGL2:s}:null}(this._canvas);if(!t)return;const{gl:e}=t;try{this._program=g(e,"\nprecision mediump float;\n\nattribute vec2 a_seed;\nattribute float a_phase;\nattribute float a_sizeSeed;\nattribute vec3 a_color;\n\nuniform float u_time;\nuniform mediump int u_concept; // precision pinned explicitly — GLSL ES 1.00 defaults int to highp in the vertex stage but mediump in the fragment stage, and this uniform is now read in both (see FRAGMENT_SRC); a bare \"uniform int\" here would silently mismatch the fragment shader's default and fail to link the whole program.\nuniform float u_deg;\nuniform float u_speed;\nuniform float u_sizeMin;\nuniform float u_sizeMax;\nuniform vec2 u_resolution;\nuniform float u_dpr;\nuniform vec2 u_pointer;\nuniform float u_pushEnabled;\nuniform float u_pushRadius;\nuniform float u_pushStrength;\n\nvarying vec3 v_color;\nvarying float v_alpha;\nvarying float v_rot;\nvarying float v_size;\n\nvoid main() {\n    float rad = radians(u_deg);\n    vec2 dir = vec2(sin(rad), cos(rad));\n\n    vec2 pos = a_seed * 2.0 - 1.0;\n    float t = u_time * 0.001 * u_speed;\n    float size = mix(u_sizeMin, u_sizeMax, a_sizeSeed);\n    float alpha = 1.0;\n    v_rot = 0.0;\n\n    if (u_concept == 1) {\n        // bubbles: rise along dir, wrap, gentle wobble, slow alpha pulse.\n        // travel wraps via fract() — a hard positional teleport once per\n        // cycle. edgeFade fades alpha to 0 just before/after that wrap point\n        // so the teleport happens while invisible instead of visibly popping.\n        // Phase driven by a_phase, NOT a_seed.x — a_seed.x also sets this\n        // particle's static pos.x below, and at dir.x == -1 exactly (deg=270)\n        // the two uses of a_seed.x cancelled out algebraically, collapsing\n        // every particle onto the same x each frame (a visible vertical line\n        // instead of a scattered field). a_phase is an independent per-particle\n        // random value, so no direction can ever reintroduce that cancellation.\n        float travel = fract(t * 0.05 + a_phase);\n        pos += dir * (travel * 2.0 - 1.0);\n        pos.x += sin(t * 1.3 + a_phase) * 0.04;\n        float edgeFade = smoothstep(0.0, 0.08, travel) * smoothstep(1.0, 0.92, travel);\n        // Floor raised to 0.6 (was 0.35) so bubbles read as solid-ish colored\n        // circles against a light background instead of a washed/pastel\n        // blob whose low-alpha core blends heavily with whatever is behind it.\n        alpha = (0.6 + 0.4 * (0.5 + 0.5 * sin(t * 2.0 + a_phase))) * edgeFade;\n    } else if (u_concept == 2) {\n        // stars: drift + gentle twinkle. The mod() wrap below can teleport a\n        // star to the opposite edge on either axis — edgeFade fades alpha out\n        // near whichever edge is closest so that teleport also happens while\n        // invisible, same idea as the bubbles wrap above. Drift coefficient\n        // (0.06, was 0.01) is fast enough to actually be visible at speed=1 —\n        // the old value was so slow it read as static even over many seconds,\n        // and a comet-trail look (trail prop, see the fade pass in JS) needs\n        // real per-frame motion to leave a visible streak.\n        pos += dir * t * 0.06;\n        pos = mod(pos + 1.0, 2.0) - 1.0;\n        // Twinkle: wide alpha/size range + a livelier frequency so stars visibly\n        // sparkle/blink rather than just gently pulse.\n        float tw = 0.5 + 0.5 * sin(t * 2.4 + a_phase * 3.0);\n        float edgeDist = min(1.0 - abs(pos.x), 1.0 - abs(pos.y));\n        float edgeFade = smoothstep(0.0, 0.06, edgeDist);\n        alpha = (0.4 + 0.6 * tw) * edgeFade;\n        size *= 0.7 + 0.35 * tw;\n    } else if (u_concept == 3) {\n        // leaf: falls/drifts along dir like bubbles, but with a wider,\n        // slower side-to-side sway (a leaf catches more air than a bubble)\n        // and a per-particle tumble rotation — a_sizeSeed picks each leaf's\n        // spin speed/direction (some barely turn, some spin either way) so\n        // the field doesn't read as uniform. The fragment shader uses v_rot\n        // to draw the actual leaf silhouette rotated to this angle.\n        // Phase driven by a_phase, not a_seed.x — see the bubbles branch above.\n        float travel = fract(t * 0.045 + a_phase);\n        pos += dir * (travel * 2.0 - 1.0);\n        pos.x += sin(t * 0.9 + a_phase) * 0.09;\n        float edgeFade = smoothstep(0.0, 0.08, travel) * smoothstep(1.0, 0.92, travel);\n        // Flat, near-opaque (no pulse, unlike bubbles) — a leaf should read\n        // as a solid, crisp cutout, not a softly breathing/translucent blob.\n        alpha = 0.98 * edgeFade;\n        float rotSpeed = (a_sizeSeed - 0.5) * 1.2;\n        v_rot = a_phase + t * rotSpeed + sin(t * 0.7 + a_phase) * 0.5;\n    } else if (u_concept == 4) {\n        // snowflake: falls almost straight down along dir with only a light\n        // sway (snow drifts far less than a leaf catching air) and a slow,\n        // steady spin — real snowflakes turn gently rather than tumbling.\n        // The fragment shader draws the actual snowflake linework rotated\n        // to v_rot.\n        // Phase driven by a_phase, not a_seed.x — see the bubbles branch above.\n        float travel = fract(t * 0.04 + a_phase);\n        pos += dir * (travel * 2.0 - 1.0);\n        pos.x += sin(t * 0.6 + a_phase) * 0.035;\n        float edgeFade = smoothstep(0.0, 0.08, travel) * smoothstep(1.0, 0.92, travel);\n        alpha = 0.9 * edgeFade;\n        float rotSpeed = (a_sizeSeed - 0.5) * 0.4;\n        v_rot = a_phase + t * rotSpeed;\n    } else {\n        // dots: no built-in motion of its own — a faint static field. The\n        // universal pointer-push below (its defining look) is what brings it\n        // to life; without push enabled it just sits at this quiet baseline.\n        alpha = 0.25;\n    }\n\n    // Universal pointer-repel push — opt-in via u_pushEnabled (the push\n    // prop), layered on top of whichever concept motion ran above so ANY\n    // concept (not just dots) can react to the pointer, with radius/strength\n    // configurable via u_pushRadius/u_pushStrength (the pushRadius/\n    // pushStrength props). Stateless: displacement and the size/alpha boost\n    // are purely a function of the CURRENT distance to u_pointer — no\n    // persisted velocity/spring state — so this stays O(1) JS cost regardless\n    // of particle count (mirrors the \"push away from cursor\" read of\n    // web-bg's pushFx without its per-tile spring integration, which would\n    // need an O(particle-count) JS loop every frame). Snaps back the instant\n    // the pointer moves away rather than bouncing/overshooting.\n    //\n    // u_pointer is in DOM/canvas pixel space (y=0 at top, growing downward);\n    // NDC y grows upward, so the ndc<->pixel conversion here flips y to\n    // match — a plain (ndc*0.5+0.5) would leave the push mirrored vertically.\n    if (u_pushEnabled > 0.5) {\n        vec2 px = vec2(pos.x * 0.5 + 0.5, 1.0 - (pos.y * 0.5 + 0.5)) * u_resolution;\n        vec2 toDot = px - u_pointer;\n        float dist = length(toDot);\n        float pushRadius = u_pushRadius * u_dpr;\n        float prox = 1.0 - smoothstep(0.0, pushRadius, dist);\n        vec2 pdir = toDot / max(dist, 1.0);\n        px += pdir * prox * prox * u_pushStrength * u_dpr;\n        vec2 n = px / u_resolution;\n        pos = vec2(n.x * 2.0 - 1.0, (1.0 - n.y) * 2.0 - 1.0);\n        size *= 1.0 + prox * 1.8;\n        alpha = clamp(alpha + prox * 0.55, 0.0, 1.0);\n    }\n\n    gl_Position = vec4(pos, 0.0, 1.0);\n    gl_PointSize = max(1.0, size * u_dpr);\n    v_color = a_color;\n    v_alpha = clamp(alpha, 0.0, 1.0);\n    v_size = gl_PointSize;\n}\n","\nprecision mediump float;\n\nuniform float u_glow;\nuniform mediump int u_concept; // must match VERTEX_SRC's precision exactly — see comment there\n\nvarying vec3 v_color;\nvarying float v_alpha;\nvarying float v_rot;\nvarying float v_size;\n\n// Distance from p to the segment a-b — shared by any custom shape that\n// needs to draw thin lines (e.g. shapeLeaf's vein below).\nfloat sdSegment(vec2 p, vec2 a, vec2 b) {\n    vec2 pa = p - a;\n    vec2 ba = b - a;\n    float h = clamp(dot(pa, ba) / dot(ba, ba), 0.0, 1.0);\n    return length(pa - ba * h);\n}\n\n// Baseline particle shape — a soft-edged circle. This is dots' own look,\n// and doubles as the shared default for every concept that doesn't define\n// its own (bubbles, stars) — only u_glow (quality-tier dependent) varies\n// the edge softness. To give a NEW concept a custom look: write its own\n// shapeXxx(p, ...) below (see shapeLeaf for the pattern — it may also\n// touch color, unlike this one) and add one branch for it in main().\n//\n// AA band width is derived from v_size (the point's actual on-screen pixel\n// diameter) rather than a fixed fraction of the shape's normalized radius —\n// a fixed band only spans a fraction of a screen pixel on large points\n// (e.g. big bubbles), which reads as a jagged/stair-stepped edge. Deriving\n// it from v_size keeps the feather pinned to ~1.5 screen pixels regardless\n// of how large or small the point is rendered.\nfloat shapeCircle(vec2 p) {\n    float dist = length(p) * 2.0;\n    float glowBoost = u_glow > 0.5 ? 1.6 : 1.0;\n    float aa = clamp(3.0 * glowBoost / v_size, 0.006, 0.5);\n    return 1.0 - smoothstep(1.0 - aa, 1.0, dist);\n}\n\n// Custom shape for u_concept == 3 (leaf): a vesica (two overlapping circles,\n// centers offset on X) pointed top/bottom, plus a short vein anchored at\n// one tip. p arrives already centered on the point (gl_PointCoord - 0.5);\n// rot is v_rot from the vertex stage, so the field looks like leaves\n// tumbling rather than a grid of identical shapes. Tints color in place\n// for the vein — custom shapes are free to touch color, not just the mask.\nfloat shapeLeaf(vec2 p, float rot, inout vec3 color) {\n    float s = sin(rot);\n    float c = cos(rot);\n    p = vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n    float r = 0.42;\n    float k = 0.30;\n    float d1 = length(p - vec2(k, 0.0)) - r;\n    float d2 = length(p - vec2(-k, 0.0)) - r;\n    float d = max(d1, d2);\n    // Tighter AA band than shapeCircle's, and tighter still than before\n    // (0.006 vs 0.01) — reads as a crisp, sharply-edged cutout.\n    float falloff = 1.0 - smoothstep(-0.006, 0.006, d);\n\n    // Vein: just a midrib (side veins read as too busy at particle scale),\n    // anchored AT the tip and tapering to nothing at veinLen. The vein is\n    // always a vertical segment at x=0, so distance-to-segment collapses to\n    // a plain y-clamp instead of general segment math.\n    float tipY = sqrt(r * r - k * k);\n    float veinLen = tipY * 1.4;\n    float veinBaseY = tipY - veinLen;\n    float ey = clamp(p.y, veinBaseY, tipY);\n    float taper = (ey - veinBaseY) / veinLen; // 1 at tip, 0 at veinLen\n    float outerR = mix(0.002, 0.012, taper);\n    float veinMask = (1.0 - smoothstep(outerR * 0.5, outerR, length(vec2(p.x, p.y - ey)))) * taper;\n    color = mix(color, vec3(1.0), veinMask * 0.3);\n\n    return falloff;\n}\n\n// Custom shape for u_concept == 4 (snowflake): 6-fold symmetric linework —\n// a short main spoke per arm with a single V-shaped fork near its tip,\n// opening outward, plus a small filled dot at the center (matches the\n// reference icon: short bold spokes, not long thin rays). Built by folding\n// p's angle into a single 60° wedge (q), so only one arm's worth of segment\n// math needs writing; the fold's built-in mirror gives the fork its\n// symmetry for free. Pure line art (no filled body, unlike shapeLeaf) with\n// a bold stroke — here the stroke IS the whole shape, so it can't afford to\n// be faint.\nfloat shapeSnowflake(vec2 p, float rot) {\n    float s = sin(rot);\n    float c = cos(rot);\n    p = vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n\n    float ang = atan(p.y, p.x);\n    float rad = length(p);\n    float sector = 1.0471975512; // 2*PI/6 — 6-fold symmetry\n    ang = mod(ang + sector * 0.5, sector) - sector * 0.5;\n    vec2 q = vec2(cos(ang), sin(ang)) * rad;\n\n    // Shorter arms than before (0.34 vs 0.46) — the reference reads as\n    // short, stubby spokes, not long thin rays.\n    float armLen = 0.34;\n    float d = sdSegment(q, vec2(0.0), vec2(armLen, 0.0));\n\n    // One V-fork near the tip, opening outward wide enough that the 3\n    // prongs (the main line's own end, plus the 2 fork tips) read as\n    // clearly separate points, not a tangle at the elbow. Anchored a bit\n    // further back from the tip (0.62 of arm length) so the fork sits\n    // lower down the arm. Wider angle than before (48° vs 40°) for more\n    // separation between the 2 branches.\n    float bAng = radians(48.0);\n    vec2 fwd = vec2(cos(bAng), sin(bAng));\n    vec2 base = vec2(armLen * 0.5, 0.0);\n    vec2 tip = base + 0.15 * fwd;\n    d = min(d, sdSegment(q, base, tip));\n    tip.y = -tip.y;\n    d = min(d, sdSegment(q, base, tip));\n\n    // Small filled dot at the very center (max(...,0.0) clamps the distance\n    // to 0 inside the dot's radius, so it reads as solid, not just a ring).\n    d = min(d, max(rad - 0.05, 0.0));\n\n    // Bold stroke centered at strokeR, with a size-adaptive AA half-band\n    // (same v_size-derived technique as shapeCircle) instead of the old\n    // fixed 0.028-0.044 band — a fixed band's screen-pixel width grows with\n    // point size, which read as soft/jagged on large snowflakes.\n    float strokeR = 0.036;\n    float halfBand = clamp(1.5 / v_size, 0.006, 0.016);\n    return 1.0 - smoothstep(strokeR - halfBand, strokeR + halfBand, d);\n}\n\nvoid main() {\n    vec2 p = gl_PointCoord - vec2(0.5);\n    vec3 color = v_color;\n    float falloff;\n\n    if (u_concept == 3) {\n        falloff = shapeLeaf(p, v_rot, color);\n    } else if (u_concept == 4) {\n        falloff = shapeSnowflake(p, v_rot);\n    } else {\n        falloff = shapeCircle(p);\n    }\n\n    if (falloff <= 0.0) discard;\n    float alpha = v_alpha * falloff;\n    gl_FragColor = vec4(color * alpha, alpha);\n}\n")}catch(n){return console.error("[svc-underlay] particle shader failed to compile/link:",n),this._program=null,void y(e)}this._glCtx=e,this._locs={a_seed:e.getAttribLocation(this._program,"a_seed"),a_phase:e.getAttribLocation(this._program,"a_phase"),a_sizeSeed:e.getAttribLocation(this._program,"a_sizeSeed"),a_color:e.getAttribLocation(this._program,"a_color"),u_time:e.getUniformLocation(this._program,"u_time"),u_concept:e.getUniformLocation(this._program,"u_concept"),u_deg:e.getUniformLocation(this._program,"u_deg"),u_speed:e.getUniformLocation(this._program,"u_speed"),u_sizeMin:e.getUniformLocation(this._program,"u_sizeMin"),u_sizeMax:e.getUniformLocation(this._program,"u_sizeMax"),u_resolution:e.getUniformLocation(this._program,"u_resolution"),u_dpr:e.getUniformLocation(this._program,"u_dpr"),u_pointer:e.getUniformLocation(this._program,"u_pointer"),u_glow:e.getUniformLocation(this._program,"u_glow"),u_pushEnabled:e.getUniformLocation(this._program,"u_pushEnabled"),u_pushRadius:e.getUniformLocation(this._program,"u_pushRadius"),u_pushStrength:e.getUniformLocation(this._program,"u_pushStrength")},this._buffer=e.createBuffer(),v(e);try{this._fadeProgram=g(e,"\nattribute vec2 a_pos;\nvoid main() {\n    gl_Position = vec4(a_pos, 0.0, 1.0);\n}\n","\nprecision mediump float;\nvoid main() {\n    gl_FragColor = vec4(0.0);\n}\n"),this._fadeBuffer=function(t){const e=t.createBuffer();return t.bindBuffer(t.ARRAY_BUFFER,e),t.bufferData(t.ARRAY_BUFFER,w,t.STATIC_DRAW),e}(e),this._fadeLoc=e.getAttribLocation(this._fadeProgram,"a_pos")}catch{this._fadeProgram=null,this._fadeBuffer=null}this._ro=new ResizeObserver(()=>this._onResize()),this._ro.observe(this),this._onResize(),this._rebuildParticles(),this._visible&&c.add(this)}_teardownGL(){if(this._ro?.disconnect(),this._ro=null,this._glCtx&&(this._buffer&&this._glCtx.deleteBuffer(this._buffer),this._program&&this._glCtx.deleteProgram(this._program),this._fadeBuffer&&this._glCtx.deleteBuffer(this._fadeBuffer),this._fadeProgram&&this._glCtx.deleteProgram(this._fadeProgram),y(this._glCtx)),this._glCtx=null,this._program=null,this._buffer=null,this._locs=null,this._fadeProgram=null,this._fadeBuffer=null,this._fadeLoc=null,this._canvas){const t=this._canvas,e=t.cloneNode();t.replaceWith(e),t.removeEventListener("webglcontextlost",this._handleContextLost),t.removeEventListener("webglcontextrestored",this._handleContextRestored),this._canvas=e,this._canvas.addEventListener("webglcontextlost",this._handleContextLost),this._canvas.addEventListener("webglcontextrestored",this._handleContextRestored)}}_onResize(){if(!this._canvas||!this._glCtx)return;const t=this.getBoundingClientRect();this._rect=t;const e=_[this._qualityCtl.tier],n=Math.min(window.devicePixelRatio||1,e.dprCap),s=Math.max(1,t.width),i=Math.max(1,t.height);this._canvas.width=Math.round(s*n),this._canvas.height=Math.round(i*n),this._dpr=n,this._glCtx.viewport(0,0,this._canvas.width,this._canvas.height),this._glCtx.clearColor(0,0,0,0),this._glCtx.clear(this._glCtx.COLOR_BUFFER_BIT)}_comSizeRange(){const t=(this.size||"").split("~").map(Number);return 2===t.length&&t.every(Number.isFinite)?t:[1,3]}_rebuildParticles(){const t=this._glCtx;if(!t)return;const e=_[this._qualityCtl.tier],n=this.density??f[this.concept]??1;let o=Math.max(8,Math.min(2e3,Math.round(e.particleBase*n)));this.limit>0&&(o=Math.min(o,this.limit));const r=function(t,e,n,s){const o=a(t,e,n),r=new Float32Array(3*s);for(let a=0;a<s;a++){const t=o[Math.random()*o.length|0],[e,n,s]=i((t.h+(14*Math.random()-7)+360)%360,Math.min(76,Math.max(32,t.s+(12*Math.random()-6))),Math.min(92,Math.max(46,t.l+(14*Math.random()-7))));r[3*a]=e,r[3*a+1]=n,r[3*a+2]=s}return r}(s(this.tint,this),this.total,this.colorful,o),l=function(t,e){const n=new Float32Array(7*t);for(let s=0;s<t;s++){const t=7*s;n[t]=Math.random(),n[t+1]=Math.random(),n[t+2]=Math.random()*Math.PI*2,n[t+3]=Math.random(),n[t+4]=e[3*s],n[t+5]=e[3*s+1],n[t+6]=e[3*s+2]}return n}(o,r);t.bindBuffer(t.ARRAY_BUFFER,this._buffer),t.bufferData(t.ARRAY_BUFFER,l,t.DYNAMIC_DRAW),this._particleCount=o,v(t)}_tick(t){const e=this._glCtx;if(!e||!this._program||!this._canvas)return!1;var n;if(t-this._lastQualitySample>1e3&&(this._lastQualitySample=t,!this.quality&&this._qualityCtl.sample(c.getFps())&&(this._onResize(),this._rebuildParticles()),this.debug&&(n=c.getFps(),x&&(x.textContent=`FPS: ${Math.round(n)}`))),this.push){const t=this._pointerX-this._pointerXEased,e=this._pointerY-this._pointerYEased;Math.abs(t)>400||Math.abs(e)>400?(this._pointerXEased=this._pointerX,this._pointerYEased=this._pointerY):(this._pointerXEased+=.2*t,this._pointerYEased+=.2*e)}const s=this._prefersReducedMotion?0:t,[i,a]=this._comSizeRange();if(this.trail>0&&this._fadeProgram&&this._fadeBuffer){const t=Math.min(.95,Math.max(0,this.trail)/100*.95);e.blendFunc(e.ZERO,e.CONSTANT_ALPHA),e.blendColor(0,0,0,t),e.useProgram(this._fadeProgram),e.bindBuffer(e.ARRAY_BUFFER,this._fadeBuffer),e.enableVertexAttribArray(this._fadeLoc),e.vertexAttribPointer(this._fadeLoc,2,e.FLOAT,!1,0,0),e.drawArrays(e.TRIANGLES,0,3),v(e)}else e.clearColor(0,0,0,0),e.clear(e.COLOR_BUFFER_BIT);return e.useProgram(this._program),e.bindBuffer(e.ARRAY_BUFFER,this._buffer),e.enableVertexAttribArray(this._locs.a_seed),e.vertexAttribPointer(this._locs.a_seed,2,e.FLOAT,!1,28,0),e.enableVertexAttribArray(this._locs.a_phase),e.vertexAttribPointer(this._locs.a_phase,1,e.FLOAT,!1,28,8),e.enableVertexAttribArray(this._locs.a_sizeSeed),e.vertexAttribPointer(this._locs.a_sizeSeed,1,e.FLOAT,!1,28,12),e.enableVertexAttribArray(this._locs.a_color),e.vertexAttribPointer(this._locs.a_color,3,e.FLOAT,!1,28,16),e.uniform1f(this._locs.u_time,s),e.uniform1i(this._locs.u_concept,p[this.concept]??p.dots),e.uniform1f(this._locs.u_deg,this.deg||0),e.uniform1f(this._locs.u_speed,this.speed||1),e.uniform1f(this._locs.u_sizeMin,i),e.uniform1f(this._locs.u_sizeMax,a),e.uniform2f(this._locs.u_resolution,this._canvas.width,this._canvas.height),e.uniform1f(this._locs.u_dpr,this._dpr||1),e.uniform2f(this._locs.u_pointer,this._pointerXEased,this._pointerYEased),e.uniform1f(this._locs.u_glow,_[this._qualityCtl.tier].glow?1:0),e.uniform1f(this._locs.u_pushEnabled,this.push?1:0),e.uniform1f(this._locs.u_pushRadius,this.pushRadius||140),e.uniform1f(this._locs.u_pushStrength,this.pushStrength||46),e.drawArrays(e.POINTS,0,this._particleCount),!this._prefersReducedMotion}_comBlobPositions(t){const e=-22*(t-1)/2,n=[];for(let s=0;s<t;s++){const t=((this.deg||0)+e+22*s-90)*Math.PI/180;n.push({x:50+Math.cos(t)*this.distance,y:50+Math.sin(t)*this.distance})}return n}_comEllipseGradient(){const t=this._blobTints,e=t.length,n=[];if(1===e){const e=t[0];n.push(`hsla(${Math.round(e.h)}, ${Math.round(e.s)}%, ${Math.round(e.l)}%, 0) 25%`),n.push(`hsla(${Math.round(e.h)}, ${Math.round(e.s)}%, ${Math.round(e.l)}%, 0.26) 50%`),n.push(`hsla(${Math.round(e.h)}, ${Math.round(e.s)}%, ${Math.round(e.l)}%, 0) 75%`)}else{const s=t[0],i=t[e-1];n.push(`hsla(${Math.round(s.h)}, ${Math.round(s.s)}%, ${Math.round(s.l)}%, 0) 5%`);const a=60/(e-1);t.forEach((t,e)=>{n.push(`hsla(${Math.round(t.h)}, ${Math.round(t.s)}%, ${Math.round(t.l)}%, 0.26) ${(20+e*a).toFixed(2)}%`)}),n.push(`hsla(${Math.round(i.h)}, ${Math.round(i.s)}%, ${Math.round(i.l)}%, 0) 95%`)}return n.join(", ")}get _comMoveMode(){return this.blobMove&&"false"!==this.blobMove?!0===this.blobMove||"true"===this.blobMove?"swap":this.blobMove:""}_comMoveStyles(t){const e=this._comMoveMode;if(!e)return"";const n="ellipse"===this.blobType;if("swap"===e){if(n)return"@keyframes _ul_sw_e{0%,100%{transform:translate(0,0)}50%{transform:translate(5%,3%)}}";const e=t.length;return t.map((n,s)=>{const i=t[(s+1)%e];return`@keyframes _ul_sw_${s}{0%,100%{transform:translate(-50%,-50%)}50%{transform:translate(calc(-50% + ${(i.x-n.x).toFixed(2)}%),calc(-50% + ${(i.y-n.y).toFixed(2)}%))}}`}).join("")}return n?"@keyframes _ul_pu_e{0%,100%{opacity:1}50%{opacity:0.05}}":"@keyframes _ul_pu{0%,100%{opacity:var(--bg-blob-opacity,0.5)}50%{opacity:0.02}}"}_comAnimStyle(t,e){const n=this._comMoveMode;if(!n)return"";const s="ellipse"===this.blobType;if("swap"===n)return s?"animation:_ul_sw_e 6s ease-in-out infinite;":`animation:_ul_sw_${t} 5s ease-in-out infinite;`;return s?"animation:_ul_pu_e 4s ease-in-out infinite;":`animation:_ul_pu 4s ease-in-out infinite;animation-delay:${(4*t/Math.max(e,1)).toFixed(2)}s;`}_rbBlobs(){if(!this.gradient||!this._blobTints.length)return"";const e="ellipse"===this.blobType,n=e?[]:this._comBlobPositions(this._blobTints.length),s=this._comMoveStyles(n);return e?t`
                ${s?t`<style>${s}</style>`:""}
                <div class="blob-ellipse" style="background:linear-gradient(${this.deg||0}deg, ${this._comEllipseGradient()});${this._comAnimStyle(0,1)}"></div>
            `:t`
            ${s?t`<style>${s}</style>`:""}
            ${this._blobTints.map((e,s)=>t`
                <div class="blob" style="background:hsl(${Math.round(e.h)},${Math.round(e.s)}%,${Math.round(e.l)}%);left:${n[s].x}%;top:${n[s].y}%;${this._comAnimStyle(s,this._blobTints.length)}"></div>
            `)}
        `}render(){return t`
            <div class="underlay-wrapper" style="border-radius:${this.rounded};">
                ${this._rbBlobs()}
                ${this.concept?t`<canvas class="underlay-canvas"></canvas>`:""}
            </div>
        `}};function L(e){return e&&Object.keys(e).length?t`
      <svc-underlay
        .tint=${e.tint}
        .rounded=${e.rounded||"0px"}
        .blobType=${e.blobType||"circleOverlap"}
        .total=${e.total||3}
        .deg=${e.deg||0}
        .distance=${e.distance||86}
        .blobMove=${e.blobMove||""}
        ?blur=${e.blur}
        ?gradient=${e.gradient}
        ?fixed=${e.fixed}
        ?colorful=${e.colorful}
      ></svc-underlay>
    `:""}customElements.get("svc-underlay")||customElements.define("svc-underlay",C);export{L as t};