---
import LayoutAdmin from '@/layouts/LayoutAdmin.astro';
const pageTitle = 'Inventory Admin';
const pageDescription = 'Inventory admin page';
---

<LayoutAdmin title={pageTitle} description={pageDescription}>
	<div class='p-4'>
		<h1 class='text-xl font-bold mb-5'>Quản lý kho hàng</h1>
		<svc-admin id='admin-inventory' dataTable='inventory' lang='vi' orderable></svc-admin>
	</div>

	<script>
		import '@/webs/auth/svc-admin.js';
		import schema from '@/services/schemas/admin/inventory.js';

		document.addEventListener('astro:page-load', () => {
			const el = document.getElementById('admin-inventory');
			if (el) (el as any).schema = schema((el as any).lang || 'vi');
		});
	</script>
</LayoutAdmin>
